# AWS-optimized requirements for KrushiVerse
# Removes heavy ML dependencies for smaller EC2 deployment

# Core FastAPI
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Google AI (primary LLM and embeddings)
google-generativeai>=0.3.2
langchain-google-genai>=0.0.6

# LangChain core
langchain>=0.1.0
langchain-community>=0.0.16

# Vector store
chromadb>=0.4.0

# Security and utilities
slowapi>=0.1.0
requests>=2.28.0

# Core dependencies
numpy>=1.21.0,<2.0.0

# Remove heavy packages for AWS:
# - sentence-transformers (uses Google embeddings instead)
# - torch (not needed with Google Gemini)
# - transformers (Google handles this)
# - CUDA packages (cloud deployment)